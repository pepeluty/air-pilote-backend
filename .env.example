# PostgreSQL
PG_HOST=postgres
PG_PORT=5432
PG_DB=air-pilote
PG_USER=postgres
PG_PASSWORD=postgres

# JWT (use a strong random string in production: openssl rand -hex 32)
JWT_ACCESS_SECRET=

# Server
PORT=3000
NODE_ENV=production